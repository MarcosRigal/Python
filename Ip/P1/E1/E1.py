import numpy as np

print("This program calculates the circumference based on the radius entered by terminal.")
radius = float(input("Enter a value for the radius: ")) 
print("The area is: ", np.pi * np.power(radius, 2), "cm²")
